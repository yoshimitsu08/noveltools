# Ledger（売上・顧客管理）制作管理資料

`noveltools` リポジトリ内の `sales_ledger.html`。noteで販売中の「しーば４」「藤岡香織」など複数ブランドの売上・顧客管理ツール。

---

## 目的・背景

- noteの管理画面からダウンロードできる販売履歴CSVを取り込み、売上・顧客・作品の管理を行いたい、という要望から着手。
- Manuscript（小説執筆管理）とは目的が異なる（執筆 vs 経理・顧客管理）ため、データ構造・保存領域とも完全に分離した独立ツールとして新規作成した。
- noveltools内での位置づけ：`index.html`のアプリ一覧から`sales_ledger.html`へ遷移する構成（yoshiapps側は汎用ツール用、noveltools側は小説関連ツール用、という運用ルールに沿う）。

---

## データ保存方針（重要）

「短期記憶」「長期記憶」という表現でユーザーから提示されたが、正しくは**キャッシュ（作業用一時データ）と正本データ（確定データ）の関係**にあたる。

- **localStorage（キャッシュ）**：作業中の一時バッファ。ブラウザデータを消すと失われる前提。
- **正本データ（ダウンロードフォルダのJSONファイル）**：ユーザーの手元にのみ存在する確定データ。GitHub等クラウドには一切アップロードしない方針（コード自体は公開リポジトリで管理するが、実データ・保存先の情報はコードに含めない）。
- **運用フロー**：作業開始時に前回エクスポートのJSONをインポート → noteのCSVを取り込んで追記・手動メモも追記 → 作業終了時に全データをJSONでエクスポート → 次回はそのファイルから再開、の繰り返し。
- **エクスポート漏れ対策**：`ledgerData.meta.exportedFlag`で「未保存の変更があるか」を管理し、`false`のときはヘッダーに警告バッジを常時表示する。
- **Excel確認用CSV書き出し**：正本データ（JSON）とは完全に無関係な、閲覧専用の一方向エクスポート。フラグ管理の対象外。

---

## データ構造

```
ledgerData = {
  version: 1,
  brands: [ {id, noteName, brandName} ],     // id は自動生成（work_xxxxx形式）
  works:  [ {id, title, brandId, memo, aliases} ], // id はユーザーが手入力する任意ID（英数字・ハイフン・アンダーバーのみ）。aliasesは表記ゆれの別タイトル文字列の配列
  sales:  [ {
    txId,            // note側の取引ID。売上の一意キー・重複防止に使用
    datetime,        // note形式 YYYYMMDDhhmmss の文字列のまま保持
    buyerNameRaw,     // CSV上の購入者名（ログイン購入はアカウント名、未ログインは"ゲスト"）
    workId,           // 紐づく作品ID（null = 未紐づけ）
    contentNameRaw,   // CSV上のコンテンツ名（表記ゆれ調査・未紐づけ時の表示用）
    issuerRaw,        // CSV上の発行事業者名（ブランド紐づけ調査用）
    amount, amountExTax, tax,
    paymentMethod,
    customerId,       // 紐づく顧客ID（null = 未紐づけ）
    guestChecked       // true = 「ゲストの正体は調査不要」と手動マーク済み
  } ],
  customers: [ {id, displayName, memo, createdAt} ],
  meta: { lastImportAt, lastExportAt, exportedFlag }
}
```

### 作品IDについて（重要な設計変更）

初期実装では作品IDもシステムの自動生成（`work_タイムスタンプ_乱数`形式）だったが、「CSVの作品タイトル表記ゆれが多いため、作品ごとに任意のIDを割り振り、以後はIDで管理したい」という要望を受けて変更。

- 作品登録フォームの一番上に「作品ID」入力欄を追加（例：`FK-001`, `shiba4_02`）
- バリデーション：英数字・ハイフン（`-`）・アンダーバー（`_`）のみ許可（`isValidWorkId()`関数）。日本語や記号はエクスポート時の安全性を考慮して不可とした。
- 重複チェック：既存の作品IDと同じ値は登録エラーとして弾く
- 一覧表示にもIDをタイトルの上に表示（ゴールド色、`font-ui`）
- **注意**：ID自体の後からの変更機能は現状ない。IDを変えたい場合は削除して登録し直す運用。

### 売上データの重複防止・突合キーについて

- 重複防止キーは「取引ID（txId）」のみ。同じ作品・同日・同じ購入者（ゲスト含む）の複数購入があっても、note側で取引ごとに毎回異なる取引IDが発行されることをユーザーが実データで確認済み（2026-08-22時点）。したがって取り違え・上書きのリスクはない。
- 「購入者名＝ゲスト」の取引は、note側の仕様上、同一人物かどうかをシステム側で自動判定する材料がない（アカウント名が出ない）。**同一人物の複数回ゲスト購入は、その都度すべて手動で顧客に紐づける必要がある**（仕様として合意済み。将来的な自動一括紐づけ機能は現状スコープ外）。

---

## 画面構成（6タブ）

| タブ | 内容 |
|---|---|
| ダッシュボード | 総売上・取引件数・顧客数のサマリー、ブランド別売上（横棒グラフ風）、作品別ランキング上位5 |
| インポート/エクスポート | 前回JSONの読み込み、noteのCSV取り込み（追記型・重複自動スキップ）、JSON保存（正本データ）、Excel用CSV書き出し（閲覧専用） |
| 売上一覧 | 全取引を新しい順に表示。作品・顧客が未紐づけの場合はその旨を警告色で表示 |
| 顧客一覧 | 購入額の多い順。カードタップで購入履歴とメモを確認・編集（`prompt`ベースの簡易UI） |
| 作品マスタ | 作品登録（ID・タイトル・ブランド・メモ）を上、ブランド登録（ブランド名・note表示名）を下に配置。CSV由来の未登録候補は`datalist`で入力補助 |
| 未紐づけ | 「作品の未紐づけ」（タブバッジでカウント対象）と「ゲスト購入者の未特定」（バッジ対象外、`guestChecked`で調査不要マーク可）を扱う |

### 未紐づけバッジの設計判断

当初は「作品未紐づけ」「顧客未紐づけ（ゲスト）」の両方を件数バッジに含める想定だったが、ユーザーからの実態ヒアリングにより、**ゲスト購入が想定より多く、かつ追跡不能なケース（退会済み等）もある**ことが判明。バッジを両方カウントすると常時大きい数字になり警告として機能しなくなるため、**バッジは「作品の未紐づけ」のみをカウントし、ゲスト未特定は「調査不要」マークで個別に除外できる方式**に変更した。

---

## 表記ゆれ対策（作品の別名＝エイリアス機能）

「顧客名・作品タイトル・ブランド名（note表示名）の登録時、些細な誤字で判別不能にならないように」という要望と、その後の「CSVの表記ゆれタイトルにIDをどう割り振るか」という議論を経て、以下の2段構えで実装した。

### 第一弾：CSV由来の候補リスト機能

読み込み済みCSVに含まれていて、まだマスタ未登録の値を`<datalist>`で入力候補として提示する機能。`getUnregisteredContentNames()` / `getUnregisteredIssuerNames()` / `getUnregisteredBuyerNames()` の3関数で算出。購入者名候補からは「ゲスト」を除外。

### 第二弾：作品の別名（エイリアス）機能

候補リストだけでは「表記ゆれのたびに未紐づけタブでの手動選択が発生し続ける」という運用コストの問題が残ることが、ユーザーとの対話で判明。以下の設計方針の違いを整理した上で、**方式2（作品は1レコード、別名リストで管理）を採用**した。

| | 方式1：表記ゆれタイトルにそれぞれ同じIDを直接付与 | 方式2：作品は1レコードのみ、別名リストを持つ（採用） |
|---|---|---|
| メリット | CSV取込→ID付与の対応が直感的 | 作品マスタが1作品＝1行で綺麗に保たれる。集計も自然に正しく合算される |
| デメリット | 作品マスタ上に同一作品が表記の数だけ複数行として並び、一覧性が悪化。集計時に結局「同じIDのものをまとめる」処理が必要になり、方式2と同程度の複雑さが裏側に発生する | 「別名」という概念が増える分、仕組みは方式1よりやや複雑（ただしユーザー操作自体は同程度） |

**実装内容**：
- `works`データに`aliases`（別名の生タイトル文字列の配列）を追加
- 作品の検索は`findWorkByTitleOrAlias()`関数に一本化。タイトル本体との完全一致、または`aliases`内のいずれかとの完全一致で該当作品を返す。CSV自動紐づけ（`importNoteCSV`）・作品登録後の再紐づけ（`relinkUnmatchedSales`）・候補リスト算出（`getUnregisteredContentNames`）はすべてこの関数経由に統一した
- **自動別名登録のトリガー**：「未紐づけ」タブで、ある売上を既存の作品にドロップダウンで紐づけたとき、その売上の生タイトル（`contentNameRaw`）が紐づけ先の作品タイトルと**異なっていれば**、自動的にその作品の`aliases`に追加する。同時に、他に同じ表記で未紐づけのままだった売上があれば一括で再紐づけする（`relinkUnmatchedSales`を呼び出し）
- **効果**：一度手動で紐づけた表記は、次回以降のCSV取込で自動的に同じ作品IDに紐づくようになり、「毎回未紐づけタブで選び直す」というコストが解消される
- 作品マスタの一覧カードに「別名（表記ゆれ）」セクションを追加し、登録済みの別名を一覧表示。誤って紐づけてしまった場合は個別に「解除」でき、解除すると該当する売上も未紐づけに戻る
- Node.js単体テストで、①表記Aは作品登録時に自動紐づけ、②表記Bは手動紐づけ時に別名として自動記憶、③別名登録後に表記Bを含む新しいCSVを取り込むと自動でIDに紐づく、の一連の流れを確認済み

**運用方針**：2026-08-22時点では方式2のまま実運用を開始し、使用感を見ながら再検討することでユーザーと合意。データ構造自体は方式1的な見せ方（別名ごとに独立行として一覧表示するオプション等）にも後から対応可能な設計にしてある。

---

## 更新履歴

| 日付 | 内容 |
|---|---|
| 2026-08-22 | 新規作成。データ構造設計、6タブ構成、note CSVパース（BOM対応・引用符対応の自前パーサー）、取引IDによる重複排除、作品マスタでの自動紐づけ（タイトル完全一致）を実装しデプロイ。noveltoolsの`index.html`にリンクカードを追加。 |
| 2026-08-22（続1） | ①**タブバー（リボンメニュー）が画面によって非表示になる不具合を修正**。原因は`nav.tabbar`および内部の`button`要素の高さが特定条件下でゼロになっていたこと（開発者ツールで実測しユーザーと共に特定）。`nav.tabbar`に`min-height:44px`・`flex-shrink:0`、ボタンに`display:inline-flex; align-items:center; line-height:1.4`を明示指定して解消。`header.app-header`にも同様に`flex-shrink:0`を保険で追加。②作品マスタの表示順を「作品→ブランド」に変更（作品登録の方が圧倒的に頻度が高いため）。③CSV由来の未登録候補を`datalist`で選択できる機能を追加（作品タイトル・note表示名・購入者名の3箇所）。 |
| 2026-08-22（続2） | 作品IDをシステム自動生成から**ユーザー任意入力方式**に変更。英数字・ハイフン・アンダーバーのみ許可のバリデーション、重複チェックを実装。作品一覧カードにIDを表示するよう変更。 |
| 2026-08-22（続3） | **作品の別名（エイリアス）機能を実装**。「同じ作品がCSV上で複数の表記ゆれを持つ場合、どうIDに集約するか」という設計議論の末、「表記ゆれタイトルにそれぞれ同じIDを直接付与する方式」と「作品は1レコードのみとし別名リストで表記ゆれを吸収する方式」を比較し、後者を採用（詳細は下記セクション参照）。未紐づけタブでの手動紐づけ時に自動で別名登録され、以後のCSV取込では自動的に同じ作品IDに紐づくようになった。作品マスタに別名一覧の表示・個別解除機能も追加。 |

---

## 既知の制約・今後の検討事項

- [ ] 別名（エイリアス）機能の実運用での使用感確認。ユーザーとは「一旦このまま実際に使ってみて、不便を感じたら再検討する」ことで合意（2026-08-22時点）
- [ ] ゲスト購入者の同一人物一括紐づけ機能（現状は1件ずつ手動、仕様として合意済みだが件数が多い場合は再検討の余地あり）
- [ ] ブランドIDも作品IDと同様に任意入力方式に揃えるかどうか（現状はブランド数が少ないため自動生成のまま据え置き）
- [ ] 顧客詳細編集が`prompt()`ベースの簡易UIのため、メモが長くなってきた場合はモーダル化を検討
- [ ] Playwright等によるブラウザ実機テストがサンドボックス環境のネットワーク制限で実行不可だったため、ロジック部分はNode.js単体テストで検証。UI崩れ系の不具合は実機確認に依存する状態が続く見込み
