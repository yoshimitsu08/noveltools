# Manuscript 制作管理資料（続き・7）

本ファイルは `10_manuscript_制作管理資料-6-1.md` の続き。コンテキスト圧迫のための分割で、内容は連続している。

---

## 更新履歴（続き）

| 日付 | 内容 |
|---|---|
| 2026-08-22 | **ダッシュボードヘッダー再編**。①「凡例（進捗ステータス説明）」「ストレージ使用量」「ピン留めタイトルバナー」を、ヘッダー下の1エリアで2秒ごとに自動ローテーション表示する方式に変更（タップでの手動送り・現在位置を示すドット付き）。従来はスマホ幅で3ブロックが縦に積み上がり、場所を取っていた。②「＋新規作品」ボタンをツールバーからアプリタイトル右側（ヘッダー行内）に移動。③ソート機能をハンバーガーメニュー内「🔀 並べ替え」（タップでサブメニュー展開）に統合、ツールバー自体を廃止。④ハンバーガーメニューの項目を「作品操作／ツール／設定」の3カテゴリに区切り線で整理。PC幅専用だった`dash-header-row`統合CSS（2026-08-22朝に追加されたばかりのもの）は本改修で不要となり削除。 |
| 2026-08-22（続1） | **ローテーション表示の見た目改善**。①高さ固定：3枚のスライドを`position:absolute`で重ねる方式に変更し、表示中の項目によってヘッダーの高さが伸縮する不具合（ピン留めバナー表示時だけ縦に膨らむ）を解消。②右→左スライドアニメーション（0.35秒、opacity＋translateX）を追加し、切替が瞬間的だった点を改善。 |
| 2026-08-22（続2） | **リールの無限ループ化**。「リストの先頭と末尾を繋げたい」という要望に対し、カードの位置計算を単純な差分（`i - centerIndex`）から循環最短距離（末尾と先頭が隣り合う扱い）に変更。中央インデックスの管理もmod演算による循環方式にし、末尾から次へ送ると先頭に戻る／その逆、が成立するようにした。ドラッグ・慣性スワイプ・ホイール・タップ、既存の操作すべてが自動的に循環対応となった。 |
| 2026-08-22（続3） | **リールのスナップ・アニメーション、複数回にわたる改善**。「スナップ速度が速いと上下端のカードの動きがぎこちない」という指摘を受け、原因調査と対策を段階的に実施。<br>**A案**：スナップのイージングを`cubic-bezier(0.22,1,0.36,1)`（急加速寄り）から`cubic-bezier(0.33,1,0.68,1)`（より均等）に変更。<br>**B案**：複数枚まとめて送る際、カードごとの移動距離に応じてtransition時間を個別調整（基準時間〜最大1.6倍の範囲で比例配分）し、移動距離が長いカードだけ見かけの速度が上がる問題を緩和。<br>両案とも部分的な改善に留まり、根本原因は「全カードを常にDOM上に保持する（全部顕現）設計上、画面外にいた見えないカードが新しく表示される瞬間、位置だけ先に動いて中身が追いつかず“ワープ”して見える」ことと判明。対症療法として新規に現れるカードのopacityフェードインを遅延させる対応も行ったが、根本解決には至らなかった。 |
| 2026-08-22（続4） | **リール大改修：全部顕現方式→電光掲示板方式へ変更**。上記の根本原因を受け、「常に全カードをDOM上に持ち、距離で位置計算する」設計そのものを刷新。①常に「画面幅に応じた表示枚数＋前後バッファ2枚ずつ」のみをDOM上に保持する「窓（ウィンドウ）」方式に変更（`REEL_BUFFER=2`、表示枚数は`reelSideCount()`で画面幅から可変算出）。②中央インデックスが動くたびに、窓の外に出たカードをDOMから削除し、新しく窓に入るカードだけを追加する。③新規カードは「窓のさらに外（見えない位置）」から最終位置までtransitionで滑り込ませることで、自然な出現演出にした。④疑似3D演出（`scale`・`rotateX`・`translateZ`・`opacity`によるCoverflow風の遠近感）自体は維持。旧来の複雑な可視判定（`REEL_VISIBLE_THRESHOLD`によるopacity/pointer-events制御）は不要になり削除。⑤画面回転等での表示枚数変化に対応するため、resizeイベントで窓を再構築する処理を追加。<br>**注**：疑似3Dは本物の3D（WebGL等によるポリゴン・メッシュ・テクスチャ）ではなく、CSSの`transform`（角度・奥行き・大きさ）とperspectiveによる見せかけの立体感である点を、資料上も明記しておく。 |
| 2026-08-22（続5） | **電光掲示板方式、動き出しのタイミング調整2件**。①新規カードの登場アニメーションが既存カードよりワンフレーム遅れて始まる問題（「見えない位置に静的配置→強制reflow→次フレームでtransition開始」という2段階処理が原因）を修正。新規カードも既存カードと完全に同じ`requestAnimationFrame`・同じタイミングでtransitionを開始するよう統一。②複数枚まとめて送る際、次候補・次々候補（＝ほとんどが新規追加カード）が一瞬遅れてついてくる問題を修正。原因は、既存カードには移動量（delta）に応じたtransition時間延長を適用していたのに対し、新規カードには常に基準時間固定を適用しており、新規カードの方が実際の移動距離が長いにもかかわらず短い時間で動こうとして間に合っていなかったこと。既存・新規の区別なく「実際に動く距離」を個別計算し、その距離に応じてtransition時間を配分する方式に統一して解消。旧`reelSetSnapDurations`関数は本改修でロジックが`reelUpdateWindowForMove`内に統合され、未使用関数として削除。<br>実機（慣性1.3・スナップ速度380ms、4枚まとめ送り含む）で確認済み、良好とのフィードバックを受けデプロイ完了。 |
| 2026-08-23 | **テーマカラー機能を新規実装**。`:root`（ダーク＝標準）に加え、`html[data-theme="xxx"]`セレクタでネイビー／墨と朱／フォレスト／プラムの4種を追加（全5種）。背景はいずれもダーク基調を維持し、`--accent`等のアクセント色系統のみ変える設計。ハンバーガーメニュー「🎨 テーマカラーを変更」から選択モーダルを開き、スウォッチ＋ラベルのリストから選択。`localStorage`に保存し次回起動時も復元。 |
| 2026-08-23（続1） | **ネタ帳「ヒラメキの種」のレイアウト改修＋自動更新間隔の可変化**。①種の表示ボックスが文章の長さに応じて可変高さになり、下の入力フォームを押し下げる不具合を修正。左ペイン内蔵の箱から、モーダルヘッダー直下・横幅いっぱいの帯（`flex-shrink:0`で高さ固定、2行`-webkit-line-clamp`）に変更。②自動更新間隔（従来3秒固定）を「操作感の調整」モーダルでスライダー変更可能に（1〜10秒）。③「操作感の調整」に全項目一括の「デフォルトに戻す」ボタンを追加。④デフォルト値を更新：スナップ速度320→600ms、編集位置オフセット90→300px（スライダー上限も拡張）。慣性1.0・透過率0.12・長押し判定0.55秒は現状維持。 |
| 2026-08-23（続2） | **ヘッダーローテーターのドット重なり不具合を修正**。原因は、ページインジケーター（`.rotator-dots`）が`.header-rotator`（`height:24px; overflow:hidden`固定）の内側最後尾に配置されており、スライドコンテンツ（凡例・容量バー・ピン留めタイトル）と縦に重なって表示されていたこと。`.header-rotator-wrap`で全体をラップし、ドットを`.header-rotator`の外側（下）に出す構成に変更して解消。あわせて、ローテーション切替間隔（従来2秒固定）を「操作感の調整」に「トピック表示の切替間隔」として追加（1〜10秒、ネタ帳の種とは独立したスライダー）。 |
| 2026-08-23（続3） | **リールカードの画像サイズ・トリミング位置調整機能**。①作品名タイトルが画像に押されて見切れる問題への対応として、画像幅を199px固定→160px固定に縮小（カード高さ112px・リール内部座標計算`REEL_STEP`等は無傷、CSSのみの変更に留めて安全性を優先）。フル可変案（画像幅・カード高さとも画面幅に応じて可変）も検討したが、リールのスクロール・スナップ計算全体の作り直しが必要になるため、静止画像モックで比較検証のうえ見送り。②画像トリミング位置（`object-position`）をユーザーがドラッグで調整できる「画像の位置を調整」モーダルを新設。カバー画像アップロード直後に自動で開く、また既存作品にも詳細メニュー「🖼️ カバー画像の位置を調整」から後付けで呼び出し可能。位置情報は`p.coverPosition = {x, y}`（%値）として作品データに保存し、リールカード・詳細プレビュー双方の`object-position`に反映。ネタ帳カードの画像は個別調整の対象外のため、`object-position:100% 50%`固定で右詰め表示に変更（キャプションが画像左寄りにデザインされている前提のため）。 |
| 2026-08-23（続4） | **PCリール左インデントの半減、カード詳細メモ欄のオートリサイズ不具合修正**。①PC幅（1024px以上）でのリール左インデント（画面左端〜カード左端の距離）を189px→95pxに変更。②カード詳細画面のメモ欄・紹介文欄（`auto-resize-textarea`）で、モーダルを開いた直後だけ実際の文字量以上に大きな余白ができる不具合を修正。原因は、高さ再計算（`input`イベント発火）がモーダルの拡大アニメーション完了前・実寸確定前に呼ばれており、`clientWidth`が正しく取得できず異常に大きい`scrollHeight`が計算されていたこと。`playDetailModalOpenAnimation`のアニメーション完了コールバック内で、モーダル内の全`auto-resize-textarea`に対し改めて`input`イベントを発火させ、実寸確定後に再計算する対応で解消。 |
| 2026-08-23（続5） | **使い方ガイド機能を新規実装**。リール末尾（ネタ帳カードのさらに後ろ）に、ネタ帳と同じ「疑似カード」方式（`HELP_PSEUDO_CARD`、`getSorted()`末尾に固定`push`）で「使い方ガイド」カードを追加（青ボーダーで区別、表紙はSVGのdata URL）。タップするとガイドモーダルが開き、「自力で書くフロー（4ステップ：新規作品→設定プロット入力→本文執筆→推敲）」と「AIと連携するフロー（8ステップ：新規作品→ネタ帳ラフメモ送信［近日実装予定］→AI Hubでのプロット生成／品質チェック／本文執筆／推敲→Sentence Selectorでの最終推敲→紹介文・画像プロンプト生成）」をタブで切り替え可能なステップカード形式で表示。既存のネタ帳タブUI（`.idea-tab-nav`/`.idea-tab-btn`/`.idea-tab-page`）のクラス構造をそのまま流用。 |
| 2026-08-23（続6） | **GitHub保存・同期機能を新規実装（第1段階）**。詳細は下記「GitHub同期機能の設計メモ」参照。要約：AES-GCM暗号化＋Fine-grained PATで、1作品＝1ファイルとしてGitHubリポジトリ（`noveltools`）に手動保存・取得できる機能。ハンバーガーメニューに初期設定モーダル、作品詳細メニューに「☁️ GitHub保存・同期」を追加。SHAベースの楽観的ロックによる競合検知（409）の基本形を実装。デプロイ後、422エラー（SHA未指定）とモーダル重なりバグ（遷移元`detailModalOverlay`の`active`外し忘れ、既知バグパターンの再発）を発見・修正済み。 |
| 2026-08-23（続7） | **422エラーの根本解決、GitHub同期モーダルの遷移修正、新端末向け作品取り込み機能を実装**。詳細は下記「GitHub同期：422エラーの根本解決」「GitHub同期：モーダル遷移修正」「GitHub同期：新端末向け作品取り込み機能」参照。①`ghPushProject`のPUT成功直後に、新shaが実際に読み取れるまで短間隔でポーリングする`ghWaitForPropagation()`を新設し、GitHub API側の反映遅延に起因する422・「ファイルなし」誤判定を解消（実機確認済み）。②GitHub同期モーダルを閉じた際（×ボタン・背景タップ・保存/取得成功後）、必ず元の作品詳細モーダルへ戻るよう修正（従来はダッシュボードへ遷移していた）。③ハンバーガーメニューに「☁️ GitHubから作品を取り込む」を新設。リポジトリ内の全同期ファイルを列挙・復号し、タイトル一覧から選んで新端末に取り込める機能（既存作品と同一IDの場合は上書き確認）。 |
| 2026-08-23（続8） | **「未記録なのに既存ファイルあり」の無言自動上書きを廃止**。詳細は下記「GitHub同期：未記録時の無言上書きを廃止」参照。要約：従来、`_githubSync`が空（このデバイスでは一度も同期していない）状態でGitHub上に既にファイルが存在するケースでは、422エラー回避のため無言でSHAを補完し強制上書きしていた。これは「他デバイス／他経路で保存済みのデータを、確認なく上書きしてしまう」危険な穴だったため、`ghPushProject`にconfirmedOverwriteフラグを追加し、未確認の場合は保存を一旦中断してneedsConfirmationを返す方式に変更。呼び出し元（保存ボタン）で内容を明示した警告ダイアログを表示し、ユーザーが承認した場合のみ保存を実行する。 |
| 2026-08-23（続9） | **作品IDをリールカード・カード詳細に表示**。リールカードのカバー画像右下に、ID先頭7文字＋「…」を控えめなバッジ（`rc-id-badge`）で表示。カード詳細モーダルには紹介文欄の下にフルIDをテキスト表示（`detailProjectIdText`、コピー機能なし）。ネタ帳・使い方ガイドの疑似カードは対象外。エクスポート/インポートでIDが変わる仕様（「新規作成」「インポート」時に新規発行、GitHub連携の保存/取得/取り込みでは既存IDを保持）を、ヨッシーさんとの質疑応答を通じて再確認・明文化した上での実装。 |

---

## GitHub同期機能の設計メモ（2026-08-23新設）

### 目的・全体方針

ローカル→Telegram送信→別端末でインポート、という手動の端末間同期フローを、GitHubリポジトリを介した同期に置き換える。リアルタイム同期は不要（手動トリガーの同期で十分という前提）。ローカルデータ全滅時の「緊急インポート」経路も兼ねる。

### 保存先・認証

| 項目 | 内容 |
|---|---|
| リポジトリ | `yoshimitsu08/noveltools`（public） |
| 認証 | Fine-grained PAT（対象リポジトリ限定・Contents: Read and write のみ。Metadata: Read-onlyは自動付与） |
| PAT保存先 | ブラウザの`localStorage`（`manuscript_github_config_v1`キー、owner/repo/暗号鍵/PATをまとめて保存） |
| 暗号化 | AES-GCM（Web Crypto API）。パスフレーズからPBKDF2（150,000回、SHA-256）で鍵導出。salt・ivは暗号文と一緒に保存し、復号時に再利用 |
| 公開リポジトリを選んだ理由 | GitHub PagesからPrivateリポジトリを読むにはトークンの扱いが複雑になるため、「public repo＋暗号化」の組み合わせをユーザー了承のうえ採用。index.htmlから辿れない・URLはアプリ内でしか分からない・中身は暗号化済み、という多層の低リスク性を踏まえた判断 |

### ファイル命名規則

- **新形式**：`manuscript_sync_<作品ID>_<タイトル>.json`（タイトルは`exportProject()`と同じサニタイズ処理・30文字上限）
- **旧形式**（2026-08-23第1段階の初期実装時点）：`manuscript_sync_<作品ID>.json`（タイトル無し）
- 同期対象の特定は常に**作品IDのプレフィックス一致**で行う（`ghFindExistingFile()`が新旧両形式を横断的に検出）。タイトルを変更して保存すると、新ファイル名でPUT→成功後に旧ファイル名をDELETEする「リネーム」処理が自動で走る

### 競合検知（SHAベースの楽観的ロック）

- 各作品は`p._githubSync = { lastSyncedSha, lastSyncedAt, lastSyncedUpdatedAt }`を持つ（`_lastSavedUpdatedAt`とは別物・保存対象）
- PUT時、`lastSyncedSha`が記録されていればそれを`sha`パラメータとして送る。GitHub側のSHAと不一致なら409（Conflict）が返り、他端末による更新を検知できる
- `lastSyncedUpdatedAt`とローカルの`p.updatedAt`を比較し、「前回同期後にローカルで未反映の変更があるか」を判定（`ghHasUnsyncedLocalChanges()`）。作品詳細の「GitHub保存・同期」モーダルを開いた際にこの判定を実施（起動時の全作品一括チェックは、作品数増加時のAPI呼び出し増加・体感速度低下を避けるため見送り。GitHub APIレート制限は認証済みで5000回/時間のため実用上は問題ないが、UXの観点で個別チェックを採用）

**既知の穴（未解消・次回対応予定）**：422エラー（「既存ファイルなのにSHA未指定」）対策として、`p._githubSync`が空（一度も同期記録がない）の場合に限り、その場でリモートの実SHAを自動取得して補完する処理を追加した。これは422自体は解消するが、**このケースに限り競合検知が機能せず、強制上書きになる**。例：デバイスAで保存済みの作品を、`_githubSync`未記録のデバイスBから保存すると、警告なしにAの内容を上書きしてしまう。次回のUI実装（下記）と合わせて、「SHA未記録でも対象ファイルが存在するなら、無言補完せずユーザーに確認を挟む」形に修正する。

### 緊急インポートとの違い

- 通常の同期：SHA突き合わせ・競合検知あり
- 緊急インポート（未実装）：ローカルデータ全滅時を想定し、同期ロジックを介さないシンプルなGET＋復号のみの経路。安全性より復旧の確実性を優先する設計方針

### UI配置

| 場所 | 内容 |
|---|---|
| ハンバーガーメニュー（ダッシュボード） | 「☁️ GitHub保存の設定」→ owner/repo・暗号鍵・PAT入力、接続テスト、保存・削除。初回利用時の重要注意事項（PATの性質・localStorage保存・共有端末での使用禁止・暗号鍵紛失時の復号不可・公開リポジトリである旨）を明示 |
| 作品詳細モーダルのドロップダウン | 「☁️ GitHub保存・同期」→ 状態表示（未同期の変更有無／リモートSHA変化の有無）、「GitHubに保存」「GitHubから取得」ボタン |

### 既知の課題・次回改修予定

| 優先度 | 内容 |
|---|---|
| ~~高~~ 解決済み | ~~422対策で追加した「SHA未記録時の自動補完」が競合検知をすり抜ける穴~~ → 2026-08-23（続8）で修正済み（下記参照） |
| 中 | SHA競合（409）発生時の「差分を見て取捨選択」UI（project内フィールド単位、特に`manuscript`は章ID単位まで） |
| 中 | 緊急インポート機能（同期ロジックなしのシンプルGET経路。新設の「新端末向け作品取り込み機能」が実質的な代替になっている可能性あり、要否は次回検討） |
| ~~低~~ 解決済み | ~~GitHub同期モーダルを閉じた（キャンセルした）際、遷移元の詳細モーダルに戻さずダッシュボードへ戻ってしまう点~~ → 2026-08-23（続7）で修正済み |

---

## GitHub同期：422エラーの根本解決（2026-08-23〔続7〕）

前回セッションで発生していた422エラー（`"sha" wasn't supplied`）・「保存直後なのにファイルが見つからない」誤判定について、原因を特定し解決した。

### 根本原因

`cache: 'no-store'`はブラウザ側キャッシュ対策としては正しかったが、それでも解消しなかった理由は**GitHub API自体の結果整合性（反映遅延）**にあった。PUTでファイル保存が成功した直後、その内容がGET（一覧取得・個別ファイル取得とも）に反映されるまで、GitHub側の内部で数百ミリ秒〜数秒のラグが生じることがある。ブラウザキャッシュの問題ではなく、GitHubのサーバー側の仕様に起因する現象だった。

### 対応内容

- `ghWaitForPropagation(cfg, filename, expectedSha, maxTries, intervalMs)` を新設。PUT成功時に返る新しい`sha`を引数に取り、対象ファイルを0.5秒間隔・最大4回（＝最大2秒）だけ再取得し、`sha`が一致するまで（＝実際に反映されたことを確認できるまで）待つ。上限に達しても反映未確認のまま処理は継続する（保存自体は成功しているため、ここで失敗扱いにはしない）。
- `ghPushProject()`のPUT成功後・`return`直前にこの確認処理を組み込んだ。呼び出し元（保存ボタン）の「保存中…」表示は、この確認処理の完了まで自動的に継続する。
- 「一定時間待つ」のではなく「実際に反映されたことを確認する」方式を採用した理由：待ち時間を固定にすると、遅延がそれより長い場合にまだ直らず、短い場合は無駄に待たせてしまうため。
- 実機確認済み：対応後、422エラーは再現しなくなった。

---

## GitHub同期：モーダル遷移修正（2026-08-23〔続7〕）

### 問題

GitHub保存・同期モーダルを閉じる（×ボタン・背景タップ・保存/取得成功時のいずれも）と、元々開いていた作品詳細モーダルへ戻らず、ダッシュボードが表示されてしまっていた。

### 対応内容

- モーダルを閉じる4箇所（×ボタン、背景タップ、Push成功時、Pull成功時）すべてに `openDetail(p.id)`（もしくは`currentGithubSyncProjectId`ベースでの呼び出し）を追加し、必ず元の詳細モーダルへ戻るよう統一。
- 実機確認済み（保存・取得・×閉じるの3パターンとも良好）。

---

## GitHub同期：新端末向け作品取り込み機能（2026-08-23〔続7〕新設）

### 目的

従来の「GitHubから取得（Pull）」ボタンは、その端末の`projects`配列に**既に同じ作品IDが存在する場合しか使えない**仕様だった（`projects.findIndex()`で対象を探すため）。そのため、「新しい端末で、まだ一度も作ったことのない作品をGitHub上から新規に持ってくる」というユースケースに対応できていなかった。この機能はその穴を埋めるもの。

### 実装内容

| 項目 | 内容 |
|---|---|
| 呼び出し口 | ハンバーガーメニュー「☁️ GitHubから作品を取り込む」（新設） |
| コアロジック | `ghListAllSyncFiles(cfg)`：リポジトリ直下の`manuscript_sync_*.json`を列挙。`ghFetchRemoteByFilename(cfg, filename)`：ファイル名を直接指定して復号取得（既存の`ghFetchRemote`はID経由の検索を前提とするため、ファイル名起点の新関数として分離） |
| 一覧表示 | 該当ファイル全件を復号し、タイトル・最終更新日を一覧表示（想定件数：数件〜数十件程度のため全件復号方式を採用。数百件規模になった場合は再設計が必要） |
| 取り込み判定 | 一覧項目タップ時、その作品IDが端末の`projects`にすでに存在するかを判定。存在すれば「この端末にあり」バッジを表示し上書き確認、存在しなければ新規追加確認 |
| 復号失敗時 | 暗号鍵が異なる場合など個別ファイルの復号に失敗しても、その項目だけ「⚠️ 復号に失敗」と表示し、一覧全体の表示は継続する |

### 今後の検討事項

- 緊急インポート機能（資料内で従来「未実装」としていたもの）は、この機能とほぼ同じ経路（GitHub上から選んで取得）になるため、別途実装するか、この機能をもって代替とするかは次回検討する。

---

## GitHub同期：未記録時の無言上書きを廃止（2026-08-23〔続8〕）

### 背景・問題

以前の422エラー対応の過程で、「`_githubSync`が空（このデバイスからは一度も同期したことがない）にもかかわらず、GitHub上には既に対象作品のファイルが存在する」というケースに対し、422エラーを回避するためその場でリモートの実SHAを自動取得して補完し、**無言で上書き保存**する処理を暫定的に入れていた。

この状態が起こりうる具体例として、「ローカルデータが全滅した端末で、GitHub上のファイルを手動でダウンロード・復号し、正規のインポート機能とは別経路で読み込んだ場合」が挙げられる（この経路自体は現状のManuscriptには実装されていないが、将来的な手動復旧作業などで起こりうる）。このケースでは、他デバイスが正規のGitHub連携で保存したデータがあっても、**競合検知が完全にすり抜けて、警告なしに上書きされてしまう**という、実質的に最も危険な穴だった。

### 対応内容（シンプル版：確認ダイアログ方式を採用）

差分UI（フィールド単位でどちらを採用するか選べる本格版）は工数がかかるため、今回はシンプルに「上書き前に必ず内容を明示した確認を挟む」方式を先行実装した。

- `ghPushProject(cfg, p, expectedSha, confirmedOverwrite)`に第4引数`confirmedOverwrite`を追加。
- 「未記録なのに既存ファイルがある」状態を検知した際、`confirmedOverwrite`が`true`でなければ、その場でSHAを補完して保存を進めるのではなく、**保存処理そのものを中断**し、`{ ok: false, needsConfirmation: true, remoteSha, filename }`を呼び出し元に返すよう変更。
- 保存ボタンのクリックハンドラ側で`needsConfirmation`を受け取った場合、状況（このデバイスは未同期であること、GitHub上に既存データがあること、上書きすると元に戻せないこと）を明示した確認ダイアログを表示。「先にGitHubから取得することを推奨」の一文も含めた。
- ユーザーが確認ダイアログで承認した場合のみ、`confirmedOverwrite=true`を付けて`ghPushProject`を再実行し、実際に保存する。
- 確認ダイアログの表示中に、さらに他デバイスが上書きしていた場合に備え、再実行時に409が返るケースのハンドリングも追加済み。

### 今後の検討事項

- 今回はシンプル版（確認ダイアログのみ）に留めたため、「どちらの内容を採用するか、フィールド単位で選べる」という本格的な差分UIは引き続き未実装（優先度：中、資料内の既知の課題テーブル参照）。
- 現状のManuscriptには「GitHubから直接手動ダウンロードして中身を読み込む」経路自体が実装されていないため、実際にこの警告が表示される場面は限定的（主に、今後手動復旧の仕組みを作った場合や、想定外の経路で`_githubSync`が欠落したデータが生じた場合への備え）。

---

## GitHub同期：デプロイ後に発生した422エラーの調査経緯（2026-08-23、資料作成後に発生）

上記「GitHub同期機能の設計メモ」作成直後の実機テストで、422エラー（`"sha" wasn't supplied`）が複数回再発。原因調査と対応の経緯を時系列で記録する（**根本原因は未確定**のまま次回に持ち越すため、詳細に残す）。

### 発生と対応の流れ

1. **1回目の422**：タイトル変更→保存の直後に発生。原因を「アプリ内部がSHAを未記憶なのに、実際にはファイルが存在するケースを考慮していなかった」ためと判断し、`ghPushProject`に「SHA未記憶時はその場で実SHAを取得して補う」処理を追加。→ デプロイ。
   - **この対応には安全上の見落としがあった**：SHA未記憶時に無言で自動補完すると、他端末が既に保存済みの内容を、競合検知なしに上書きしてしまう可能性がある（ユーザー指摘により発覚）。「次回改修（差分UI実装）と合わせて塞ぐ」課題として一旦保留し、このリスクを認識した状態で先に進めることをユーザーと合意。
2. **2回目の422**：同一の操作パターン（タイトル変更→保存）で再発。「保存ボタンを連打したのでは」という仮説のもと、`ghSyncInProgress`フラグによる二重実行防止・Push/Pullボタンの相互排他ロックを追加。→ デプロイ。
3. **3回目の422**：ユーザーが再現条件を明確化——「タイトル変更→保存→（保存成功後、自動で）ダッシュボードに戻る→同じ作品のGitHub同期モーダルを開く→『GitHub上にファイルがありません』と表示される→保存ボタンを押す→422」。**連打ではなく、直前の保存成功にもかかわらず次に開いた際に「ファイルなし」と誤認する**という、より深刻な現象と判明。
   - 調査の結果、GitHubリポジトリ上には実際にファイルが存在していることを確認（`manuscript_sync_<ID>_<その時点のタイトル>.json`が実在）。つまり、**保存自体は成功しているが、アプリ側が「存在しない」と誤判定している**状態。
   - 候補として（a）`ghFindExistingFile`が呼ぶ一覧取得API・個別ファイル取得APIがブラウザ側でキャッシュされ、直前のPUT/DELETEが反映される前の古い結果を参照している可能性、（b）`p._githubSync`のsave自体が何らかの理由で実は失敗・巻き戻っている可能性、の2つを検討。
   - 一旦（a）を有力候補とし、`ghApiRequest`・`ghFindExistingFile`の`fetch`呼び出しに`cache: 'no-store'`を追加してデプロイ。**この時点では効果が確認できておらず、次回チャットで実機確認から再開する。**
4. **並行して追加しかけた「422自動リトライ」処理を撤去**：422発生時にその場で実SHAを取得し直して自動的に1回リトライする処理を一時追加したが、ユーザーから「原因未確定のまま自動で握りつぶすのは、本当に競合が起きているケースでも黙って上書きしてしまう危険がある」と指摘があり、その場で撤去。**「エラーが出ない」ことと「安全である」ことは別物であり、原因の切り分けが済むまでは自動フォールバックを入れない**という方針をこの時点で確認した。

### 解決済み（2026-08-23〔続7〕）

- `cache: 'no-store'`だけでは不十分で、根本原因はGitHub API側の反映遅延（結果整合性）だった。`ghWaitForPropagation()`による反映確認ポーリングで解決。詳細は上記「GitHub同期：422エラーの根本解決」参照。
- **既知の穴（2026-08-23〔続8〕で解決済み）**：SHA未記憶時の自動補完が競合検知をすり抜ける問題は、無言上書きの廃止・確認ダイアログ方式への変更で解消した。詳細は上記「GitHub同期：未記録時の無言上書きを廃止」参照。



## リール（作品カード一覧）の設計メモ（2026-08-22時点）

一連の改修で設計が大きく変わったため、現状の仕組みを整理しておく。

### DOM構成の考え方

- **旧方式（〜2026-08-22朝）**：全作品分のカードを常にDOM上に生成し、それぞれが「中心から何枚離れているか」を自力計算してtransformを決める「全部顕現」方式。作品数が多くても全部DOMにある。
- **新方式（2026-08-22改修後）**：常に「表示枚数（画面幅で可変）＋前後バッファ2枚ずつ」だけをDOM上に保持する「窓」方式（通称：電光掲示板方式）。中心が動くたびに、窓の外に出たカードをDOMから削除し、新しく窓に入る分だけ追加する。カードの識別は配列インデックスではなく`data-id`属性（作品ID）で行う。

### 疑似3D演出について

現在のCoverflow風の見た目（中心が大きく正面、離れるほど小さく・傾いて・奥へ）は、本物の3Dモデル（WebGLのポリゴン・メッシュ・テクスチャ）ではなく、CSSの`transform`（`rotateX`・`translateZ`・`scale`）とopacityを、中心からの相対位置（`offset`）に応じて計算しているだけの「見せかけの3D」である。DOM構成（全部顕現／窓方式）と見た目の演出（3Dか平面か）は別レイヤーの話であり、今回はDOM構成だけを窓方式に変更し、見た目の演出は維持する形で決着した。

### 主要な関数

| 関数 | 役割 |
|---|---|
| `renderReel()` | 並び順の変化を検知し、変化があれば`reelRebuildWindow()`（窓の完全作り直し）、なければ`reelRefreshWindowContent()`（中身の差し替えのみ）を呼ぶ |
| `reelRebuildWindow()` | 現在の中心インデックスを基準に、窓に必要なカードをアニメーションなしで一括生成する（初期表示・並び替え直後用） |
| `reelGoTo(index, animate)` | 中心インデックスを循環（mod演算）で切り替え、移動量（delta）を`reelUpdateWindowForMove()`に渡す |
| `reelUpdateWindowForMove(delta, animate)` | 窓の入れ替え本体。既存カードのoffsetシフト・範囲外カードの削除・新規カードの追加・全カード共通のtransitionタイミング制御を行う |
| `reelPositionCards(dragOffsetPx)` / `reelPositionSpecificCards(...)` | offsetに基づき疑似3Dのtransformを計算・適用する |
| `reelSideCount()` | 画面幅から「中心の片側に何枚見せるか」を返す（狭い画面=1＝表示3枚相当、広い画面=2＝表示5枚相当） |

### 既知の制約・今後の調整余地

- ドラッグ中にバッファ（前後2枚）を超えて指を引っ張ると、窓の外は何もない空白になる（意図的な仕様。違和感が出るようなら`REEL_BUFFER`の値を増やす想定）
- 一度に送れる枚数の上限は4枚（暴走防止、`reelFinishDrag`内の`cardsToMove`のclamp値）。窓のバッファ枚数と合わせて調整する
- 表示枚数の画面幅ブレークポイント（380px未満／700px未満で判定）は暫定値、実機での見え方次第で調整余地あり

---

## 作品ID（`p.id`）の発行ルール（2026-08-23〔続9〕整理）

質疑応答を通じて改めて確認・明文化した内容。今後の設計判断の基準として残す。

### ID発行方式

`uid()`関数（`'p' + Date.now() + Math.random().toString(36).slice(2,8)`）により、`p`+タイムスタンプ+ランダム文字列（16〜20文字程度）が生成される。

### 新しいIDが発行される操作

| 操作 | 挙動 |
|---|---|
| 新規作成（`newProjectSkeleton()`） | 呼ばれるたびに新規ID発行 |
| ローカルインポート（`importProjectFromFile`） | ファイル内のIDをそのまま使わず、新規IDを発行し直す。この端末の`projects`配列内でのID重複を避けるための設計 |

### 既存IDが保持される操作

| 操作 | 挙動 |
|---|---|
| GitHub連携「保存」（Push） | 同一作品の別バージョンとして扱うため、IDは変更しない |
| GitHub連携「取得」（Pull） | 同上 |
| GitHub連携「新端末向け作品取り込み」 | 取り込み判定自体がID一致を基準にしているため、IDを保持する（変えると機能が成立しない） |

### 実務上の含意

「作品Aをエクスポート→ローカルインポート→GitHubに保存」を行うと、元の作品Aとは**別ID・別ファイル**としてGitHub上に並ぶ（中身は保存時点のコピーだが、以後は別々に更新されていく independent な作品として扱われる）。「同じ内容の作品を意図的に複製・派生させたい」場合はこの経路で問題ないが、「同じ作品を別デバイスへ移したいだけ」の場合は、ローカルエクスポート/インポートではなくGitHub連携（保存→他端末で取得、または新設の「GitHubから作品を取り込む」）を使うのが正しい。この違いをヨッシーさんが誤って踏まないよう、使い方ガイド等での言及も将来的に検討の余地あり。

---

## TODO（追加分）

- [ ] 今回一連のリール改修（無限ループ・電光掲示板方式・アニメーション調整）の実機での継続的な動作確認（特に大きい端末回転、超大量作品でのバッファ超え挙動）
- [ ] リールの表示枚数ブレークポイント（380px/700px）の妥当性を実機で見直す
- [ ] 一度に送れる枚数の上限（4枚）とバッファ枚数（前後2枚）のバランスを、運用しながら調整
- [x] ヘッダーローテーション表示（凡例／ストレージ／ピン留め）の切替間隔・ドット操作の使い勝手を実機で確認 → 2026-08-23、ドット重なり不具合を修正、切替間隔も可変化（「操作感の調整」内「トピック表示の切替間隔」）。実機確認待ち
- [ ] テーマカラー機能（5種）を実機で確認。他画面（詳細・Sentence Selector等）への反映状況、再起動後の保持を含む
- [ ] ネタ帳「ヒラメキの種」のヘッダー帯化・自動更新間隔スライダーを実機で確認
- [ ] リールカード画像160px化・トリミング位置調整モーダルを実機で確認。特に既存作品への「後付け位置調整」動線の分かりやすさ
- [ ] カード詳細メモ欄オートリサイズの修正を実機で確認（開いた直後の余白が適切な高さになっているか）
- [ ] 使い方ガイドの内容（特にAI連携フロー側の各ステップ文言）をヨッシーさんと一緒に確認・調整。現状は叩き台
- [ ] ネタ帳「ラフメモ→作品カードへ送る」機能の実装（使い方ガイド内で「近日実装予定」と案内済みのため、実装時はガイド文言も追随して更新すること）
- [ ] フル可変案（カード画像幅・高さとも画面幅連動、リール内部座標計算の作り直し）は今回見送り。将来的にニーズが強まった場合の再検討候補として残す
